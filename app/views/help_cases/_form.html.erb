<%= form_for(@help_case) do |f| %>
  <% if @help_case.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@help_case.errors.count, "error") %> prohibited this help_case from being saved:</h2>

      <ul>
      <% @help_case.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
